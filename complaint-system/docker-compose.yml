# version: "3.8"

# services:
#   mysql:
#     image: mysql:8.4
#     container_name: complaint-mysql

#     environment:
#       MYSQL_ROOT_PASSWORD: ${DATASOURCE_PASSWORD}
#       MYSQL_DATABASE: complaint_db

#     ports:
#       - "3306:3306"

#     volumes:
#       - mysql-data:/var/lib/mysql

#   complaint-system:
#     build: .
#     container_name: complaint-system

#     depends_on:
#       - mysql

#     ports:
#       - "8080:8080"

#     env_file:
#       - .env

#     restart: unless-stopped

# volumes:
#   mysql-data:

version: "3.8"

services:
  complaint-system:
    build: .
    container_name: complaint-system

    ports:
      - "8080:8080"

    env_file:
      - .env

    restart: unless-stopped